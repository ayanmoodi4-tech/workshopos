module {
  public type Timestamp = Int;
  public type WorkerId = Nat;
  public type JobId = Text; // Format: JOB-YYYYMMDD-NNN
  public type ProductId = Nat;
  public type AuditEntryId = Nat;
  public type AssignmentId = Nat;
};
